const accountId = 123456
let accountEmail = "anuj@test.com"
console.table([
    accountId ,accountEmail
]);
 /*
always prefer using let and use var because of issue of scope and function cope 
const -- cannot be change one we define its 
let --- change its after define 
 */


/* -- DATATPYE-- 
1 - Number 
2-  String -> like this ""
3-  boolean -> ture oR False
4-  null -> Standalond value and when we check the datatype like this console.log(typeof null) its give the object
5- undefine -> and we check the undefine its give undefine 
and the main different between of null and undefine is for Example
      when we write  let accountid;-> the output of this code is undefine 
       and we write let accountId = null -> their output is empty and give any value 

5- symbole -> unique
6 - object -> 
and the Note is when we check the datatype using [typeof]----
*/